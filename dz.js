class Transport {
constructor(color, speed, titel){
    this.color = color;
    this.speed = speed;
    this.titel = titel;
}
     startEngine(){
        console.log( "is started");
}
}

class Car extends Transport{
       
    }
startEngine (`${this.titel}"takeoff"`) 
    console.log("takeoff");

const airplane = new Transport(
   
)
Transport.startEngine("  is started!");

const helicopter = new Transport(
   
)
helicopter.startEngine("is started!");

